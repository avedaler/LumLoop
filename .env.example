# LumLoop Environment Variables
# Copy to .env and fill in values

# Database (PostgreSQL) — required for production
# For DigitalOcean managed DB, this is auto-injected
DATABASE_URL=postgresql://user:password@host:5432/lumloop

# Server
NODE_ENV=production
PORT=5000

# Domain
DOMAIN=lumloop.com
